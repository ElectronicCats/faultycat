"""faultycmd — host tool for FaultyCat v3 (BSD-3-Clause).

The CLI entry point lives in :mod:`faultycmd.cli`; the TUI in
:mod:`faultycmd.tui`. Wire-protocol primitives shared across all
clients live in :mod:`faultycmd.framing` and per-protocol modules
under :mod:`faultycmd.protocols`.
"""

__version__ = "0.1.0"
